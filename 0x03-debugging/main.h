#ifndef MAIN_H_INCLUDED
#define MAIN_H_INCLUDED

#include <stdio.h>
 
void positive_or_negative(int);
int largest_number(int, int, int);
void print_remaning_days(int day, int month, int year);
int convert_day(int, int);

#endif
